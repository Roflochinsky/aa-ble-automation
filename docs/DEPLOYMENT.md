# Руководство по развёртыванию AA_BLE Automation

## Содержание

1. [Обзор системы](#обзор-системы)
2. [Требования](#требования)
3. [Настройка Google Cloud](#настройка-google-cloud)
4. [Создание Telegram-бота](#создание-telegram-бота)
5. [Настройка конфигурации](#настройка-конфигурации)
6. [Работа с Git и GitHub](#работа-с-git-и-github)
7. [Запуск](#запуск)
8. [Развёртывание на сервере](#развёртывание-на-сервере)
9. [Справочник CLI](#справочник-cli)
10. [Troubleshooting](#troubleshooting)

---

## Обзор системы

### Что делает система

AA_BLE Automation автоматизирует создание отчётов по данным BLE-маячков:

1. **Загружает** Excel-файлы AA_BLE из папок Google Drive (по одной папке на площадку)
2. **Получает** справочные данные из Google Sheets (Журнал BLE, Привязка людей)
3. **Генерирует** интерактивные HTML-отчёты с SVG-таймлайнами
4. **Отправляет** готовые отчёты в Telegram-чат

### Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                        Google Drive                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │ MAGNIT_AUTO  │  │MAGNIT_BUTOVO │  │MAGNIT_LOMONOS│  ...      │
│  │  (входные    │  │  (входные    │  │  (входные    │           │
│  │   Excel)     │  │   Excel)     │  │   Excel)     │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AA_BLE Automation                             │
│                                                                  │
│  1. Загрузка данных из GDrive/GSheets                           │
│  2. Нормализация и обработка                                    │
│  3. Построение минутных сегментов                               │
│  4. Генерация SVG-таймлайнов                                    │
│  5. Формирование HTML-отчёта                                    │
└─────────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
┌─────────────────────────┐    ┌─────────────────────────┐
│  Google Sheets          │    │  Telegram Bot           │
│  (справочники)          │    │                         │
│  • Журнал BLE           │    │  📄 HTML-отчёты         │
│  • Привязка людей       │    │  📊 Уведомления         │
└─────────────────────────┘    │  ⚠️ Ошибки              │
                               └─────────────────────────┘
```

### Формат выходных отчётов

Каждый отчёт — это HTML-файл с:
- **Оглавлением** — список всех сотрудников
- **KPI-карточками** — Зоны работ / Перерывы / Вне BLE / Прочее (в минутах и %)
- **SVG-таймлайном** — интерактивная диаграмма с навигацией ◀ ▶
- **Детализацией** — accordion с разбивкой по зонам и меткам

Имя файла: `AA_BLE_{площадка}_{дд-мм-гггг}.html`

---

## Требования

### Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| Python | 3.10 | 3.11+ |
| RAM | 512 MB | 1 GB |
| Диск | 500 MB | 1 GB |
| Сеть | Доступ к Google API и Telegram | — |

### Необходимые аккаунты

- **Google Cloud** — для доступа к Drive и Sheets API
- **Telegram** — для создания бота и получения отчётов

### Python-зависимости

```
pandas>=2.0.0
openpyxl>=3.1.0
python-telegram-bot>=20.0
google-api-python-client>=2.0.0
google-auth>=2.0.0
python-dotenv>=1.0.0
```

---

## Настройка Google Cloud

### Шаг 1: Создание проекта

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект: **aa-ble-automation**
3. Запомните Project ID

### Шаг 2: Включение API

В разделе **APIs & Services → Library** включите:
- ✅ Google Drive API
- ✅ Google Sheets API

### Шаг 3: Создание сервисного аккаунта

1. **IAM & Admin → Service Accounts → Create Service Account**
2. Имя: `aa-ble-automation`
3. Описание: `Service account for AA_BLE report automation`
4. Нажмите **Create and Continue** → **Done**

### Шаг 4: Создание ключа

1. Откройте созданный сервисный аккаунт
2. Вкладка **Keys → Add Key → Create new key**
3. Формат: **JSON**
4. Сохраните файл как `credentials.json`

> ⚠️ **Важно**: Не добавляйте `credentials.json` в git! Добавьте в `.gitignore`.

### Шаг 5: Предоставление доступа

Скопируйте email сервисного аккаунта из `credentials.json` (поле `client_email`).

Предоставьте доступ этому email:

| Ресурс | Тип доступа |
|--------|-------------|
| Папки с входными файлами (для каждой площадки) | Читатель |
| Таблица "Журнал BLE" | Читатель |
| Таблица "Привязка людей" | Читатель |

### Шаг 6: Получение ID ресурсов

**ID папки Google Drive:**
```
https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz
                                        └─────────────────────────┘
                                              ↑ это ID папки
```

**ID таблицы Google Sheets:**
```
https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz/edit
                                       └─────────────────────────┘
                                              ↑ это ID таблицы
```

---

## Создание Telegram-бота

### Шаг 1: Создание бота через @BotFather

1. Откройте Telegram на телефоне или компьютере
2. В поиске введите `@BotFather` и откройте этот чат
3. Нажмите **Start** (или отправьте `/start`)
4. Отправьте команду: `/newbot`
5. BotFather спросит имя бота — введите: `AA BLE Reports`
6. BotFather спросит username — введите: `aa_ble_reports_bot`
   - Username должен заканчиваться на `bot` или `_bot`
   - Должен быть уникальным
7. BotFather пришлёт сообщение с токеном:
   ```
   Done! Congratulations on your new bot...
   Use this token to access the HTTP API:
   123456789:ABCdefGHIjklMNOpqrSTUvwxYZ
   ```
8. **Скопируйте и сохраните токен** — он понадобится для конфигурации

### Шаг 2: Создание группы и добавление бота

#### 2.1 Создание группы

**На телефоне (Android/iOS):**
1. Откройте Telegram
2. Нажмите на иконку ✏️ (новое сообщение) в правом нижнем углу
3. Выберите **Новая группа**
4. Добавьте хотя бы одного участника (можно себя с другого аккаунта или коллегу)
5. Введите название группы: `AA_BLE Отчёты`
6. Нажмите ✓ (галочку) для создания

**На компьютере (Telegram Desktop):**
1. Откройте Telegram Desktop
2. Нажмите ☰ (меню) в левом верхнем углу
3. Выберите **Создать группу**
4. Добавьте участника
5. Введите название: `AA_BLE Отчёты`
6. Нажмите **Создать**

#### 2.2 Добавление бота в группу

**На телефоне:**
1. Откройте созданную группу
2. Нажмите на название группы вверху экрана
3. Нажмите **Добавить участников** (или иконку 👤+)
4. В поиске введите username вашего бота: `@aa_ble_reports_bot`
5. Нажмите на бота в результатах поиска
6. Подтвердите добавление

**На компьютере:**
1. Откройте группу
2. Нажмите на название группы вверху
3. Нажмите **Добавить участников**
4. Введите `@aa_ble_reports_bot`
5. Выберите бота и нажмите **Добавить**

#### 2.3 Назначение бота администратором

Это нужно, чтобы бот мог отправлять файлы в группу.

**На телефоне:**
1. Откройте группу
2. Нажмите на название группы
3. Нажмите на имя бота в списке участников
4. Нажмите **Сделать администратором** (или **Права администратора**)
5. Включите право **Отправка сообщений**
6. Нажмите ✓ (сохранить)

**На компьютере:**
1. Откройте группу → нажмите на название
2. Найдите бота в списке участников
3. Правой кнопкой мыши → **Назначить администратором**
4. Включите **Отправка сообщений**
5. Нажмите **Сохранить**

### Шаг 3: Получение Chat ID группы

Chat ID — это уникальный идентификатор группы, куда бот будет отправлять отчёты.

#### Способ 1: Через браузер (рекомендуется)

1. **Отправьте любое сообщение в группу** (например, "test")
2. Откройте браузер и перейдите по ссылке (замените `<TOKEN>` на ваш токен):
   ```
   https://api.telegram.org/bot<TOKEN>/getUpdates
   ```
   Пример:
   ```
   https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrSTUvwxYZ/getUpdates
   ```
3. В ответе найдите блок с вашим сообщением:
   ```json
   "chat": {
     "id": -1001234567890,
     "title": "AA_BLE Отчёты",
     "type": "supergroup"
   }
   ```
4. **Скопируйте значение `id`** — это Chat ID (например: `-1001234567890`)

> ⚠️ Chat ID группы всегда **отрицательный** и начинается с `-100`

#### Способ 2: Через @RawDataBot

1. Добавьте бота [@RawDataBot](https://t.me/RawDataBot) в группу
2. Бот автоматически отправит сообщение с информацией о чате
3. Найдите строку `"id":` — это Chat ID
4. **Удалите @RawDataBot из группы** (он больше не нужен)

#### Способ 3: Через @userinfobot

1. Добавьте [@userinfobot](https://t.me/userinfobot) в группу
2. Бот покажет Chat ID
3. Удалите бота из группы

### Шаг 4: Проверка работы бота

Проверьте, что бот может отправлять сообщения:

```bash
curl -X POST "https://api.telegram.org/bot<TOKEN>/sendMessage" \
  -d "chat_id=<CHAT_ID>" \
  -d "text=Тестовое сообщение от AA_BLE бота"
```

Или в PowerShell (Windows):
```powershell
Invoke-RestMethod -Uri "https://api.telegram.org/bot<TOKEN>/sendMessage" -Method Post -Body @{chat_id="<CHAT_ID>"; text="Тестовое сообщение"}
```

Если в группе появилось сообщение — всё настроено правильно!

---

## Настройка конфигурации

Система использует два файла конфигурации:

| Файл | Назначение |
|------|------------|
| `.env` | Глобальные настройки (можно переопределить через переменные окружения) |
| `facilities_config.json` | Конфигурация площадок |

**Приоритет:** Переменные окружения → `.env` → `facilities_config.json` → defaults

### Шаг 1: Создание .env

```bash
cp .env.example .env
```

Заполните `.env`:

```ini
# Google Cloud
GOOGLE_CREDENTIALS_PATH=credentials.json

# Google Sheets (общие справочники)
GSHEETS_BLE_JOURNAL_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz
GSHEETS_BLE_JOURNAL_SHEET=Sheet1
GSHEETS_PEOPLE_MAPPING_ID=1ZyXwVuTsRqPoNmLkJiHgFeDcBa
GSHEETS_PEOPLE_MAPPING_SHEET=Sheet1

# Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ
TELEGRAM_CHAT_ID=-1001234567890

# Обработка (опционально)
TIME_WINDOW_START=6:00
TIME_WINDOW_END=23:00
```

### Шаг 2: Создание facilities_config.json

```json
{
  "global": {
    "google_credentials_path": "credentials.json",
    "telegram_bot_token": "123456789:ABCdefGHIjklMNOpqrSTUvwxYZ",
    "telegram_chat_id": "-1001234567890",
    "gsheets_ble_journal_id": "1AbCdEfGhIjKlMnOpQrStUvWxYz",
    "gsheets_ble_journal_sheet": "Sheet1",
    "gsheets_people_mapping_id": "1ZyXwVuTsRqPoNmLkJiHgFeDcBa",
    "gsheets_people_mapping_sheet": "Sheet1",
    "time_window_start": "6:00",
    "time_window_end": "23:00"
  },
  "facilities": [
    {
      "name": "MAGNIT_AUTO",
      "input_folder_id": "1FolderIdForAutoFacility",
      "enabled": true
    },
    {
      "name": "MAGNIT_BUTOVO",
      "input_folder_id": "1FolderIdForButovoFacility",
      "enabled": true
    },
    {
      "name": "MAGNIT_LOMONOSOV",
      "input_folder_id": "1FolderIdForLomonosovFacility",
      "enabled": true
    },
    {
      "name": "MAGNIT_POLIAK",
      "input_folder_id": "1FolderIdForPoliakFacility",
      "enabled": true
    },
    {
      "name": "MAGNIT_CTILIKHON",
      "input_folder_id": "1FolderIdForCtilikhonFacility",
      "enabled": true
    },
    {
      "name": "MAGNIT_CHANDAO",
      "input_folder_id": "1FolderIdForChandaoFacility",
      "enabled": false
    }
  ]
}
```

### Параметры площадки

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| `name` | Да | Название площадки (используется в имени файла) |
| `input_folder_id` | Да | ID папки Google Drive с входными Excel-файлами |
| `enabled` | Нет | Включена ли площадка (по умолчанию `true`) |

### Шаг 3: Проверка конфигурации

```bash
python -c "from src.config import ConfigManager; c = ConfigManager.load(); print(f'OK: {len(c.get_enabled_facilities())} площадок')"
```

---

## Работа с Git и GitHub

### Первоначальная настройка (локальная машина)

#### Шаг 1: Инициализация репозитория

Если репозиторий ещё не создан:

```bash
cd c:\Users\nikita.trubaev\Desktop\obxodbot\pyreport10\magnit_aa_ble

# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: AA_BLE Automation"
```

#### Шаг 2: Создание репозитория на GitHub

1. Откройте [github.com](https://github.com) и войдите в аккаунт
2. Нажмите **+** → **New repository**
3. Заполните:
   - Repository name: `aa-ble-automation`
   - Description: `Автоматизация отчётов AA_BLE`
   - Visibility: **Private** (рекомендуется для рабочих проектов)
4. **Не ставьте** галочки на README, .gitignore, license (они уже есть локально)
5. Нажмите **Create repository**

#### Шаг 3: Подключение локального репозитория к GitHub

```bash
# Добавление удалённого репозитория
git remote add origin https://github.com/YOUR_USERNAME/aa-ble-automation.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

### Настройка .gitignore

Убедитесь, что секретные файлы не попадут в репозиторий:

```bash
# Создайте или проверьте .gitignore
```

Содержимое `.gitignore`:

```gitignore
# Секретные файлы
.env
credentials.json
facilities_config.json

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
*.egg-info/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Тесты
.pytest_cache/
.hypothesis/
htmlcov/
.coverage

# Временные файлы
*.log
*.tmp
output/
*.html
```

### Обновление кода

#### На локальной машине (после изменений)

```bash
# Проверка изменений
git status

# Добавление изменений
git add .

# Коммит с описанием
git commit -m "Описание изменений"

# Отправка на GitHub
git push
```

#### На сервере (получение обновлений)

```bash
cd /opt/aa-ble-automation

# Получение изменений
git pull

# Перезапуск сервиса (если используется systemd)
sudo systemctl restart aa-ble-automation.timer
```

### Развёртывание на сервере через GitHub

#### Первоначальное клонирование

```bash
# Подключитесь к серверу по SSH
ssh user@your-server.com

# Перейдите в директорию для приложений
cd /opt

# Клонирование репозитория
git clone https://github.com/YOUR_USERNAME/aa-ble-automation.git

# Переход в директорию проекта
cd aa-ble-automation
```

#### Настройка секретных файлов на сервере

Секретные файлы **не хранятся в Git**, их нужно создать вручную на сервере:

```bash
# Создание .env
nano .env
# Вставьте содержимое из .env.example и заполните значения

# Создание credentials.json
nano credentials.json
# Вставьте содержимое JSON-ключа сервисного аккаунта

# Создание facilities_config.json
nano facilities_config.json
# Вставьте конфигурацию площадок
```

#### Установка зависимостей

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### Работа с ветками (опционально)

Для разработки новых функций:

```bash
# Создание новой ветки
git checkout -b feature/new-feature

# Работа над функцией...
git add .
git commit -m "Add new feature"

# Отправка ветки на GitHub
git push -u origin feature/new-feature

# После проверки — слияние в main
git checkout main
git merge feature/new-feature
git push
```

### Полезные команды Git

| Команда | Описание |
|---------|----------|
| `git status` | Показать изменённые файлы |
| `git diff` | Показать изменения в файлах |
| `git log --oneline` | История коммитов (кратко) |
| `git pull` | Получить изменения с GitHub |
| `git push` | Отправить изменения на GitHub |
| `git stash` | Временно сохранить изменения |
| `git stash pop` | Восстановить сохранённые изменения |

---

## Запуск

### Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv venv

# Активация
source venv/bin/activate  # Linux/macOS
.\venv\Scripts\activate   # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### Примеры запуска

```bash
# Все активные площадки за сегодня
python -m src.main

# Конкретная дата
python -m src.main --date 24.12.2025

# Диапазон дат
python -m src.main --date-from 20.12.2025 --date-to 24.12.2025

# Только определённые площадки
python -m src.main --facilities MAGNIT_AUTO MAGNIT_LOMONOSOV

# Комбинация параметров
python -m src.main --date 24.12.2025 --facilities MAGNIT_AUTO

# Альтернативный конфиг
python -m src.main --config facilities_config.prod.json
```

### Что происходит при запуске

1. ✅ Загрузка конфигурации
2. ✅ Аутентификация в Google API
3. ✅ Загрузка справочников (Журнал BLE, Привязка людей)
4. 🔄 Для каждой активной площадки:
   - Загрузка Excel-файлов из папки GDrive
   - Обработка данных и построение сегментов
   - Генерация HTML-отчёта
   - Отправка в Telegram
5. ✅ Итоговое сообщение в Telegram

---

## Развёртывание на сервере

### Вариант 1: systemd (Linux)

#### Создание service-файла

`/etc/systemd/system/aa-ble-automation.service`:

```ini
[Unit]
Description=AA_BLE Report Automation
After=network.target

[Service]
Type=oneshot
User=www-data
WorkingDirectory=/opt/aa-ble-automation
Environment="PATH=/opt/aa-ble-automation/venv/bin"
ExecStart=/opt/aa-ble-automation/venv/bin/python -m src.main

StandardOutput=journal
StandardError=journal
SyslogIdentifier=aa-ble-automation

[Install]
WantedBy=multi-user.target
```

#### Создание timer-файла

`/etc/systemd/system/aa-ble-automation.timer`:

```ini
[Unit]
Description=Run AA_BLE Automation daily at 22:00

[Timer]
OnCalendar=*-*-* 22:00:00
Persistent=true

[Install]
WantedBy=timers.target
```

#### Активация

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now aa-ble-automation.timer

# Проверка
sudo systemctl list-timers | grep aa-ble

# Ручной запуск
sudo systemctl start aa-ble-automation.service

# Логи
sudo journalctl -u aa-ble-automation.service -f
```

### Вариант 2: Docker

#### docker-compose.yml

```yaml
version: '3.8'

services:
  aa-ble-automation:
    build: .
    container_name: aa-ble-automation
    restart: "no"
    volumes:
      - ./.env:/app/.env:ro
      - ./credentials.json:/app/credentials.json:ro
      - ./facilities_config.json:/app/facilities_config.json:ro
    environment:
      - TZ=Europe/Moscow
```

#### Запуск

```bash
# Сборка и запуск
docker-compose up --build

# С параметрами
docker-compose run aa-ble-automation python -m src.main --date 24.12.2025
```

#### Cron для Docker

```cron
0 22 * * * cd /opt/aa-ble-automation && docker-compose up --abort-on-container-exit >> /var/log/aa-ble.log 2>&1
```

---

## Справочник CLI

```bash
python -m src.main [OPTIONS]
```

| Опция | Описание | Пример |
|-------|----------|--------|
| `--date` | Конкретная дата | `--date 24.12.2025` |
| `--date-from` | Начало диапазона | `--date-from 20.12.2025` |
| `--date-to` | Конец диапазона | `--date-to 24.12.2025` |
| `--facilities` | Список площадок | `--facilities MAGNIT_AUTO MAGNIT_BUTOVO` |
| `--config` | Путь к JSON-конфигу | `--config config.prod.json` |
| `--env` | Путь к .env файлу | `--env /etc/aa-ble/.env` |

### Форматы даты

Поддерживаются форматы:
- `DD.MM.YYYY` — `24.12.2025`
- `YYYY-MM-DD` — `2025-12-24`

---

## Справочник переменных окружения

| Переменная | Обязательная | Описание | По умолчанию |
|------------|--------------|----------|--------------|
| `GOOGLE_CREDENTIALS_PATH` | Да | Путь к JSON-ключу | `credentials.json` |
| `GSHEETS_BLE_JOURNAL_ID` | Да | ID таблицы "Журнал BLE" | — |
| `GSHEETS_BLE_JOURNAL_SHEET` | Нет | Имя листа | `Sheet1` |
| `GSHEETS_PEOPLE_MAPPING_ID` | Да | ID таблицы "Привязка людей" | — |
| `GSHEETS_PEOPLE_MAPPING_SHEET` | Нет | Имя листа | `Sheet1` |
| `TELEGRAM_BOT_TOKEN` | Да | Токен Telegram-бота | — |
| `TELEGRAM_CHAT_ID` | Да | ID чата для отчётов | — |
| `TIME_WINDOW_START` | Нет | Начало временного окна | `6:00` |
| `TIME_WINDOW_END` | Нет | Конец временного окна | `23:00` |

---

## Troubleshooting

### Ошибки Google API

#### `HttpError 403: The caller does not have permission`

**Причина:** Сервисный аккаунт не имеет доступа к ресурсу.

**Решение:**
1. Откройте `credentials.json`
2. Скопируйте `client_email`
3. Предоставьте доступ этому email к папке/таблице

#### `HttpError 404: File not found`

**Причина:** Неверный ID папки или таблицы.

**Решение:** Проверьте ID в URL ресурса и обновите конфигурацию.

### Ошибки Telegram

#### `Unauthorized`

**Причина:** Неверный токен бота.

**Решение:** Получите новый токен у @BotFather командой `/token`.

#### `Chat not found`

**Причина:** Бот не добавлен в чат или неверный Chat ID.

**Решение:**
```bash
curl "https://api.telegram.org/bot<TOKEN>/getUpdates"
```

#### Файлы не отправляются

**Причина:** Бот не является администратором группы.

**Решение:** Сделайте бота администратором с правом отправки сообщений.

### Ошибки конфигурации

#### `Missing required configuration`

**Причина:** Не заполнены обязательные параметры.

**Решение:**
```bash
python -c "from src.config import ConfigManager; ConfigManager.load()"
```

#### `Facilities config not found`

**Причина:** Не найден файл `facilities_config.json`.

**Решение:**
```bash
# Создайте файл или укажите путь явно
python -m src.main --config /path/to/config.json
```

### Логирование

```bash
# systemd
sudo journalctl -u aa-ble-automation.service -f

# Docker
docker-compose logs -f

# Статус таймера
sudo systemctl list-timers | grep aa-ble
```

### Fallback при недоступности Telegram

Если Telegram недоступен, отчёты сохраняются локально в рабочей директории.

---

## Структура входных данных

### Excel-файл AA_BLE

Система читает **второй лист** Excel-файла. Ожидаемые колонки:

| Колонка | Описание |
|---------|----------|
| ТН | Табельный номер сотрудника |
| День смены | Дата смены |
| BLE-метка | Номер BLE-маячка |
| Зона | ID зоны (0-13) |
| Время на объекте | Время фиксации |

### Таблица "Журнал BLE"

| Колонка | Описание |
|---------|----------|
| A | Номер метки |
| D | Описание/локация метки |

### Таблица "Привязка людей"

| Колонка | Описание |
|---------|----------|
| A | Табельный номер (ТН) |
| B | ФИО сотрудника |
| D | Участок работ |
