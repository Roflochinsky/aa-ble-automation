# AA_BLE Automation: Архитектура и Продуктовое Видение

Система автоматизированного мониторинга и отчетности на базе данных BLE-трекинга.

## 🌟 Features (Ключевые возможности)

### 1. Интеллектуальный ETL-процесс
*   **Гибридный парсинг данных:** Система умеет считывать данные из Excel-файлов, даже если заголовки повреждены или названы нестандартно (например, `metka` vs `Метка`), используя механизм **Positional Fallback** (чтение по фиксированным индексам колонок).
*   **Поддержка ночных смен:** Алгоритм корректно обрабатывает переход времени через полночь (например, `23:55` -> `00:05`), автоматически определяя смену суток без необходимости явного указания даты в каждой строке.
*   **Обогащение данных:** Данные трекинга автоматически связываются со справочниками сотрудников и описаниями BLE-меток (локаций) из Google Sheets.

### 2. Визуальная отчетность (SVG Timelines)
*   **Динамический Viewport:** Графики автоматически масштабируются от первой до последней минуты активности сотрудника.
*   **Интерактивность:** HTML-отчеты содержат встроенную навигацию, тултипы с детализацией и аккордеон для просмотра статистики по зонам.
*   **KPI Dashboard:** Мгновенный расчет времени в зонах работ, перерывах и вне зоны действия сети.

### 3. Надежность и Планирование
*   **Smart Scheduling:** Бот автоматически запускается ежедневно в **08:00**.
*   **Retry Policy:** В случае ошибки (например, файл еще не выгружен в Drive) система не "падает", а переходит в режим ожидания и повторяет попытку через 3 часа.
*   **Dockerized:** Полная изоляция окружения и зависимостей.

---

## ✅ Solved Problems (Решаемые проблемы)

| Проблема бизнеса | Решение AA_BLE |
| :--- | :--- |
| **"Слепые зоны" в данных** | Ранее "metka 0" или отсутствие заголовков делало отчеты нечитаемыми. Сейчас внедрен механизм **Deep Column Scan**, который находит данные даже в плохо форматированных файлах. |
| **Ошибка "Ночной смены"** | Ручные отчеты часто ломались на стыке суток (00:00). Система теперь видит хронологию и склеивает смену в единый непрерывный таймлайн. |
| **Сложность доступа** | Раньше отчеты лежали в папках на диске. Теперь готовый HTML-файл с графиками приходит **прямо в Telegram** ответственным лицам. |
| **Ручной перезапуск** | При отсутствии файла оператору приходилось ждать или запускать скрипт руками. Теперь бот сам **ждет появления файлов** и уведомляет об успехе. |

---

## 🚀 Proposal: Интерактивное управление (/makereport)

В текущей реализации пересчет исторических данных требует доступа к серверу и ввода CLI-команд в терминале Docker. Это неудобно для бизнес-пользователей.

**Предлагается внедрить полноценного Telegram-бота с обратной связью.**

### User Story
> Как менеджер, я хочу иметь возможность запросить отчет за прошлую неделю прямо в чате, чтобы не просить разработчика заходить на сервер.

### Техническая реализация

Необходимо трансформировать `src/clients/telegram.py` из пассивного логгера в активного слушателя (Polling или Webhook).

#### Новая команда: `/makereport`

**Синтаксис:**
`/makereport [DD.MM.YYYY] [DD.MM.YYYY]`

**Сценарии использования:**
1.  `/makereport` — пересчитать за сегодня.
2.  `/makereport 05.01.2026` — пересчитать за конкретную дату.
3.  `/makereport 01.01.2026 10.01.2026` — сгенерировать отчеты за период.

#### Архитектурные изменения

1.  **Event Loop:**
    Вместо `time.sleep()` в `main.py`, основной процесс должен запустить `bot.polling()` (используя библиотеку `pyTelegramBotAPI` или `aiogram`).

2.  **Асинхронность / Очереди:**
    Генерация отчета — тяжелая операция (IO-bound + CPU-bound).
    *   *Проблема:* Если запустить генерацию прямо в обработчике Telegram, бот "зависнет" и перестанет отвечать другим пользователям.
    *   *Решение:* Использовать `threading` или `asyncio`. При получении команды `/makereport`, бот должен:
        1. Ответить пользователю: *"Задача принята в работу, ожидайте..."*.
        2. Запустить `AABLEReportOrchestrator.run()` в отдельном потоке.
        3. По завершении отправить файл и сообщение *"Готово ✅"*.

3.  **Безопасность:**
    Внедрить проверку `User ID`. Бот должен реагировать на команды только от пользователей, указанных в `ALLOWED_USER_IDS` (в `.env`), чтобы посторонние не могли нагружать сервер.

4.  **Планировщик:**
    Так как `time.sleep` уйдет, для ежедневного запуска в 08:00 нужно использовать легковесную библиотеку планирования внутри Python (например, `schedule` или `apscheduler`), работающую параллельно с прослушиванием Telegram.

### Пример диалога

**User:** `/makereport 01.01.2026`
**Bot:** 🤖 Запрос принят. Начинаю генерацию отчета за 01.01.2026 для всех площадок...
*(спустя 30-60 сек)*
**Bot:** 📄 [Файл: AA_BLE_MAGNIT_AUTO_01-01-2026.html]
**Bot:** ✅ Готово!

### 4. Expert Track: Manual Control & Logging

Для администраторов и разработчиков предлагается расширенный режим управления, минимизирующий информационный шум.

#### Manual Mode (Ручной режим)
Возможность отключить автоматический планировщик через конфигурацию (например, `DISABLE_SCHEDULER=true` в `.env`), оставив бота работать исключительно в реактивном режиме (ответ на команды).
*   *Вариант "Только 08:00":* Жесткая привязка автозапуска только к одной временной метке, без повторных попыток (retries) в течение дня, если это создает лишний шум.

#### Команда `/logs`
Вместо отправки сообщений об ошибках (Traceback) прямо в чат, предлагается накапливать их в лог-файл и выдавать по запросу. Это сделает канал чистым для бизнес-пользователей.

**Синтаксис:**
`/logs` — получить лог последнего запуска (автоматического или ручного).
`/logs 05.01.2026` — получить архив логов за конкретную дату.

**Механизм работы:**
1.  В процессе работы `AABLEReportOrchestrator` пишет детальные логи (DEBUG/INFO/ERROR) в локальный файл `logs/app_{date}.log`.
2.  При критической ошибке в чат отправляется только краткое уведомление: *"⚠️ Произошла ошибка. Используйте /logs для деталей"*.
3.  По команде `/logs` бот читает файл и отправляет его как документ `.txt` или выводит последние 20 строк текстом.

**Преимущество:**
Разделение потоков информации: руководители видят только готовые отчеты, а инженеры могут получить полную диагностику одной командой, не заходя на сервер по SSH.
